name: Deploy to Irtaqi VPS

on:
  push:
    branches: [main]

concurrency:
  group: deploy-ringkaskajianai
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy over SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT }}
          script_stop: true
          script: |
            set -e

            APP_DIR="/var/www/ringkaskajianai.com"
            SERVICE_NAME="ringkaskajianai"

            cd "$APP_DIR"
            git fetch origin main
            git reset --hard origin/main

            /home/deploy/.bun/bin/bun install --frozen-lockfile
            /home/deploy/.bun/bin/bun run build

            sudo systemctl restart "$SERVICE_NAME"
            sudo systemctl --no-pager --full status "$SERVICE_NAME" || true
            echo "Deployment to Irtaqi VPS completed successfully."
